# black, isort y ruff se configuran en pyproject.toml. Aquí queda sólo mypy, para quien
# quiera correrlo: no viene en el venv de desarrollo porque las anotaciones ya se
# verifican con ruff (reglas ANN) y con las pruebas.
[mypy]
python_version = 3.9
ignore_missing_imports = True
